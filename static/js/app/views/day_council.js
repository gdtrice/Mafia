define([
    "react",
    "constants",
    "underscore"
],
function (
    React,
    CONSTANTS,
    _
    ) {
    // TODO: rename to kill results
    return React.createClass({
        render: function() {
            return (
                <div> view is ready for code...let's get it</div>
            );
        }
    });
});
